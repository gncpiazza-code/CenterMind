/** Altura mínima compartida paneles CC y Altas en supervisión (columna 50/50). */
export const SUPERVISION_SPLIT_PANEL_MIN_H = "min-h-[580px]";

/** Área scroll interna (resto = header + tabs). */
export const SUPERVISION_PANEL_SCROLL_MIN_H = "min-h-[420px]";
