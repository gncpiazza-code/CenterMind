"use client";

/**
 * @deprecated El mesh vive dentro de VisorWaterGlass (una sola píldora).
 * Mantener export vacío por si algo lo importa — no renderiza caja opaca.
 */
export function VisorGlassMeshBackdrop() {
  return null;
}
