// Tests básicos de SHELFYAPP.
// Los tests de integración completos se agregan a medida que se implementan
// las pantallas de cada feature.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder — tests de integración pendientes',
      (WidgetTester tester) async {
    // TODO: agregar tests cuando se implementen las pantallas de features.
    expect(true, isTrue);
  });
}
